# 智能家居 3D 演练场 — 真实 Jev + 阶跃语音版

参照官方智能家居教学视频的参考 UI 复刻的 3D 仿真屋：对它说话或打字，
一次 Jev 调用里塞进意图判断、复合检测、范围/设备定位和**全部五类设备的动作预判**（投机提示），
代码端按确定性分支剪枝，3D 场景实时执行。语音输入走阶跃星辰（StepFun）流式 ASR，
打字输入原样保留。

参考版（Downloads 里的 `typesafe_smart_home_3d_playground.html`）全部数据是写死的
关键词模拟器。本版把 Jev 真正接了进去：追踪面板里的每个概率、每毫秒延迟都是
API 真实返回、客户端实测的。

## 快速开始

```bash
cd smart-home
python3 serve_smart_home.py --port 8810     # 自动开浏览器

# 接真实 Jev（不设则页面自动用本地模拟引擎，功能一样，数据是模拟的）
export TYPESAFE_API_KEY=你的key              # console.typesafe.ai 获取

# 语音输入、复合拆分、知识问答走阶跃星辰，需要：
export STEPFUN_API_KEY=你的key               # platform.stepfun.com 获取
```

页面必须经这个本地服务打开（不能直接双击 HTML）：两个上游 API 不保证给浏览器
CORS 头，而且 key 也不能暴露在页面里，所以由它做代理。

## 语音怎么用

点输入框右侧的麦克风 → 说指令（中英文都行，自动检测）→ 再点一次结束
（或 ESC 取消，最长 15 秒自动停）→ 识别文字实时填进输入框 → 自动执行。
录音期间有红点脉冲、声浪条和计时。打字输入不受任何影响。

麦克风采集 16kHz 单声道 16-bit 裸 PCM（AudioWorklet，老浏览器自动退到
ScriptProcessor）。**说话过程中就在实时识别**：每新增约 1 秒音频，把已积累的
整段音频重识别一次（全量重识别保证文本前后一致、不切半词），文字边说边长出来；
停止后再做最终识别兜底。实测 4.5s 录音触发 3 次滚动识别，每次首字 ~0.5s；
滚动开销可忽略（ASR 按时长 0.15 元/小时计费）。这条用的是 SSE 单发接口的
"伪流式"；阶跃另有 WebSocket 双向流式模型（`stepaudio-2.5-asr-stream`，
增量结果 + 逐词时间戳），是后续零延迟上屏的升级路径。服务端转发 SSE 并把
增量事件原样透传回页面。

### 麦克风点不动/被拒绝？

浏览器对麦克风有三道门槛，页面会按实际原因给出对应提示：

1. **非安全上下文**（症状：用 `http://192.168.x.x` 局域网地址打开）：浏览器直接
   禁用麦克风。解决：`python3 serve_smart_home.py --host 0.0.0.0 --tls`，
   然后打开 `https://<本机IP>:8810`，对自签证书警告点「高级 → 继续前往」
   （服务首次会用 openssl 生成 `certs/` 下的证书）。
2. **权限被拒绝**：点地址栏左侧的锁/摄像头图标，把麦克风设为「允许」再重试。
3. **窗口不提供麦克风**：部分内嵌/小窗口浏览器不暴露 `getUserMedia`，
   换系统 Chrome/Safari 打开页面地址即可（`http://127.0.0.1` 本机地址在
   系统浏览器里测过可用）。

## 一次调用里发生了什么

和教程视频完全同款的投机提示结构，只是数据变成了真的：

1. **唯一的一次 Jev 调用**（`POST /v1/systemone`）捆绑 10 个问题：
   - `intent`（choice）：智能家居指令 / 信息请求 / 设备状态查询
   - `is_compound`（noul）：是否含多个独立操作
   - `scope` + `target_room` + `category`（choice）：范围 / 房间 / 设备类别
   - `light/fan/speaker/appliance/lock_action` 五个动作问题——**不管用户说什么都问**，
     无关的在代码端剪枝，相关的直接取用，零二次往返
2. 代码端按意图分支：
   - **信息请求** → 转交 step-3.5-flash 回答（`1989 世界大赛` → 奥克兰运动家 4:0 巨人）
   - **状态查询** → 投机答案定位设备，状态从本地 3D 状态直读，零额外调用
   - **复合指令** → step-1o-turbo-vision 拆成原子子指令，每条并行再走一次 Jev
   - **单一指令** → 直接从投机答案取相关类别的动作和目标，派发到 3D

模型分工：拆指令用 `step-1o-turbo-vision`（无推理开销，快），知识问答用
`step-3.5-flash`（推理型，准）——turbo 答 1989 世界大赛会答错成巨人队，3.5 答
对，这本身就是"按需分配算力"的现场例子。

## 和传统 LLM 对比（step-5-preview）

命令条左侧有管线切换：**Jev 投机 / 传统 LLM / 双路对比**。

- **传统 LLM**：同一段决策规则写成 JSON schema，让 `step-5-preview` 一次直答
  （意图、拆分、定位、动作、回答全在这一次输出里，拆分是它内联完成的——这是
  大模型的结构性优势，不需要编排）。页面把它的 JSON 转成与 Jev 完全相同的派发
  语义，追踪面板第 6 节显示其延迟、tokens 和原始输出。
- **双路对比**：同一指令同时发两路，第 1 节下方出现对照块——延迟、tokens、
  派发结果是否一致、LLM 慢几倍，一句话给结论。

实测（2026-09-22，Jev 侧走 mock 上游 120–260ms，量级与真实 Jev 中位 259–269ms 一致）：

| 同一指令 | Jev 投机管线 | step-5-preview 直答 | 慢多少 |
|---|---|---|---|
| 把咖啡烧上（单指令） | 171ms · 415 入/0 出 | 3089ms · 584 入+出 | **18.1×** |
| 关厨房灯锁书房门（复合） | 1413ms（含拆分+并行） | 4320ms | **3.1×** |
| 全屋灯全关（单指令） | ~230ms | ~5024ms | **~22×** |

两路在全部测试指令上**派发结果完全一致**（LLM 的 JSON 遵从度实测很好），
但传统 LLM 每单要多花几百个输出 token（按阶跃定价计费），而 Jev 输出免费、
输入按 $0.042/M tokens 计，单次约合 $0.000017。

### 成本账（页面实时显示）

单价取自官方定价页（阶跃：`docs/zh/guides/pricing/details`；Jev：console.typesafe.ai），
统一折成人民币展示（汇率假设 7.2，可在页面 `PRICING` 常量里改）：

| 模型 | 输入 (元/M tokens) | 输出 (元/M tokens) |
|---|---|---|
| Jev jev-latest | $0.042（≈¥0.30），输出**免费** | 免费 |
| step-5-preview（对比直答） | 7 | 20 |
| step-3.5-flash（知识问答） | 0.7 | 2.1 |
| step-1o-turbo-vision（拆指令） | 2.5 | 8 |
| stepaudio-2.5-asr（语音识别） | 按音频时长 0.15 元/小时 | — |

页面里看什么：

- **双路对照块**多了"成本"一行：Jev 绿色、LLM 红色，结论行给"慢几倍 / 贵几倍"
  （实测单指令 Jev ¥0.0001~0.0007 vs step-5-preview ¥0.009~0.018，**贵 13~135×**——
  区间宽是因为 5-preview 的输出长度和延迟波动都大）；
- **第 6 节**显示 LLM 单次的入/出 tokens 和本次成本（含单价）；
- **右下角页脚**每跑一条指令累加：本次成本（双路模式标注"两路合计"）、
  入/出 tokens、会话累计（次数 · 金额 · tokens）；
- **语音输入**按录音时长把 ASR 费用（0.15 元/小时）也算进会话累计，
  识别完成的横幅里直接标出来。

传统 LLM 输出的**质量方差**也值得看：问 1989 世界大赛冠军，step-3.5-flash 答
"运动家 4:0 横扫"（对），step-5-preview 答过"4:1 战胜"（比分错），turbo 答错过
冠军队——结构化路由 + 专职小模型并不只是省钱，还能按需选最准的那颗脑子。

## 串行 / 并行工作流编排（任务制）

复合指令不再一律并行。拆分之后多一次 Jev 编排调用，问两类问题（仍是单次往返）：

- 每对子指令一个 **noul**：「A 必须在 B 之前完成，B 才有意义/结果才正确吗？」
  ——同一设备动作相冲（先开后关同一盏灯）或存在因果前提 → 高概率；
  目标互不相关（厨房灯 + 书房门锁）→ 低概率。
- 每条子指令一个 **score**（0 最后 / 1 随时 / 2 最先，安全类优先）——用于同序位排序。

代码端规则：任一依赖概率 ≥ 60% → **串行执行栈**（按拓扑序 + 优先级逐个执行，
每个任务等前一个派发完才动，顺序决定最终状态）；全部低于阈值 → **并行**（原行为）。
追踪面板第 3 节下方实时显示依赖对、概率和执行栈状态（⏳/▶️/✅）。

实测（mock 上游）：「先把客厅灯打开，然后再把客厅灯关掉」→ 依赖 93% → 串行栈，
客厅灯最终为**关**（顺序保持）；「关掉厨房的灯，然后锁上书房的门」→ 无依赖 →
并行，且按优先级锁门排在关灯之前。编排分析本身只花 133–141ms。

## 实测数据（2026-09-22，本机 macOS + 真实上游）

| 项目 | 实测 |
|---|---|
| 阶跃 ASR `stepaudio-2.5-asr`（16k PCM 裸流，2.3s 中文语音） | 首个增量 0.52–0.54s，识别完成 0.56s，全文正确 |
| 阶跃 ASR `stepaudio-3-asr-max` | 首个增量 0.65s，同正确 |
| 复合拆分 `step-1o-turbo-vision` | 0.7–1.5s |
| 知识问答 `step-3.5-flash` | ≈2.6s（推理型换准确率） |
| Jev 单指令全链路（mock 上游 120–260ms） | 168–250ms，与真实 Jev 官方中位 259–269ms 同量级 |
| Jev 复合链路（同上） | 初筛 202ms + 拆分 716ms + 并行×2 359ms ≈ 1.28s |
| 一次投机调用的量级 | ~400 input tokens；按输入 $0.042/M tokens 计约 $0.000017/次，输出免费 |

功能回归（浏览器逐项验证）：开机“把咖啡烧上”→ 咖啡机开 ✓；
“关掉厨房的灯，然后锁上书房的门”→ 拆两条并行、厨房灯灭书房门锁上 ✓；
“Turn off all of the lights”→ 全屋五灯全灭 ✓；“客厅的灯现在开着吗”→
本地直读“客厅的灯：开启”✓；不配 `TYPESAFE_API_KEY` → 自动降级本地模拟，
全部功能照常 ✓。

## 踩过的坑

1. **ASR 的 `language` 字段**：传 `null` 直接 400；**整个字段省略**才是自动中英文
   检测。服务端只在明确给了语言时才带上它。
2. **MediaRecorder 的 webm 不在阶跃支持的格式清单里**（ogg/mp3/wav/pcm/m4a），
   所以不走 MediaRecorder，直接 AudioWorklet 采 16k 裸 PCM，格式最干净。
3. **推理型模型的正文在 `reasoning_content`**，`content` 是空的——服务端做了回退。
4. **浏览器直连会被 CORS 拦 + key 不能进页面**，本地代理服务是必须的，不是可选。
5. 派发映射曾把返回属性写成 `dispatched`、消费端读 `dispatchedActions`，
   `Object.assign` 对 undefined 静默吞掉——表现为“追踪面板全对、设备纹丝不动”。
   一次改名修掉。教训：这种“显示层正确、副作用丢失”的 bug，要盯数据流而不是 UI。

## 文件说明

- `serve_smart_home.py` — 本地服务：静态页 + `/api/jev`（systemone 代理）+
  `/api/asr`（SSE 透传）+ `/api/chat`（拆分/问答双模型）+ `/api/health`（模式探测）。
  支持 `TYPESAFE_BASE_URL` 覆盖上游（与官方 SDK 同名约定），联调时可指向 mock。
- `smart_home_playground.html` — 单文件页面：3D 场景（Three.js）+ 命令条
  （麦克风 + 打字）+ 决策追踪面板（真实概率/延迟/剪枝标记）+ 开关矩阵 +
  `window.smartHome` REPL。

## 已知边界

- 语音只能在 `http://127.0.0.1`（或 https）下用——浏览器的 getUserMedia 安全上下文要求；
  本地服务默认就满足。
- 说话人音量太小或环境噪杂时 ASR 可能返回空文本，页面会提示“没听清”，再试即可。
- `smart_home_query` 的设备定位依赖投机答案的 `category + target_room`，
  问法太模糊（“家里现在什么情况”）会退化为全屋状态摘要。
